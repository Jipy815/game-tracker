# Supabase Edge Functions

The deployable functions live in `supabase/functions/`, which is the layout expected by the Supabase CLI. The previous Node-oriented examples were replaced because Supabase Edge Functions run on Deno.

Deploy `process-notifications` with:

```powershell
npx supabase functions deploy process-notifications
```
